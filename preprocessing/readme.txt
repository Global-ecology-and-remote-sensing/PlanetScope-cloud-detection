These two steps are used to generate PlanetScope time-series images that have the same spatial extent
Planet_noqc_clipping (Step 1): clip PlanetScope images within the same day to the same ROI region
Planet_noqc_mosaic (Step 2): mosaic clipped PlanetScope images (derived from step 1) within the same day to the same ROI region